# ASTRA v1.4 Tamir — Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ASTRA v1.3 komut+paketini, tespit edilen çelişki/eksik/hata/makyaj bulgularını bulgu-kimliğine bağlı değişikliklerle onararak, çıkış kapıları artefakt-şartlı ve tek otorite sıralı v1.4 tek-dosya belgesine dönüştürmek.

**Architecture:** `astra/bundle_v1_4/` (v1.3 kopyasından başlar) içinde kod+test+komut metni tek kaynak olarak düzeltilir; `astra/scripts/bundle_tools.py build` gömülü belgeyi ve manifest hash'lerini yeniden üretir; `verification/` bu ortamda gerçek koşuyla yenilenir. Her görev bir bulgu kümesine bağlıdır (spec §8).

**Tech Stack:** Python 3.11+ stdlib (unittest, json, ast, fractions, subprocess, selectors), POSIX; dış bağımlılık yok; ağ yok.

## Global Constraints

- Python 3.11+ ve POSIX; yalnız stdlib (v1.3 ile aynı). Bu ortamda `python3` = 3.11.15.
- Canlı sağlayıcı çağrısı YOK (anahtar yok, egress bloklu); hiçbir test/veri "canlı doğrulandı" demez.
- Doğrulama komutu (her görevde): `cd astra/bundle_v1_4 && python3 -m unittest discover -s tests -p 'test_*.py'` — tüm testler geçmeli; v1.3'ten gelen 113 test yalnız gerekçesi CHANGELOG'a yazılarak değiştirilebilir (adı değişen test: eski ad → yeni ad + neden).
- Gizli bilgi (anahtar) hiçbir log/fiş/hata mesajına yazılmaz; hata gövdesi yazılmaz, yalnız kod + HTTP durum sınıfı.
- Komut metni Türkçe; kod yorumları İngilizce. Koruma listesi cümleleri (spec: BULGULAR_kendi_okuma.md son bölümü) v1.4 metninde anlam korunarak bulunur.
- Yeni ret kodları (birebir): `BLOCKED_ATTEMPTS_REQUIRED`, `ENVELOPE_SIZE`, `FINALIZE_ALREADY_DONE`, `SEMANTIC_REVIEW_REPLAY`, `SOURCE_TOOL_BINDING`, `REVIEW_BUDGET_EXCEEDED`, `REQUIREMENT_UNVERIFIED`, `REVIEW_PROVIDER_HTTP_4XX`, `REVIEW_PROVIDER_HTTP_5XX`, `SOURCE_SYMLINK_REJECTED`.
- Her görev sonunda commit; commit mesajı `astra v1.4: <görev>`; mesajda model adı yok.
- Bulgu kimlikleri: `K-xx` (BULGULAR_kendi_okuma.md), `<boyut>-n` (bulgular_ham_isakisi.json). Çürütme sonucu (`bulgular_hukum.json`) geldiğinde ÇÜRÜDÜ olan bulgunun görevi Task 13'te "uygulanmadı" olarak kaydedilir.
- Worktree: `/home/user/Future-/.claude/worktrees/astra-tamir-v1-4`; tüm yollar buna göredir.

---

### Task 1: v1.4 paket kopyası ve taban

**Files:**
- Create: `astra/bundle_v1_4/` (kopya: `astra/bundle_v1_3/` tamamı)
- Test: mevcut `astra/bundle_v1_4/tests/`

**Interfaces:**
- Produces: `astra/bundle_v1_4/` — sonraki bütün görevler bu dizinde çalışır.

- [ ] **Step 1: Kopyala**

```bash
cp -r astra/bundle_v1_3 astra/bundle_v1_4 && find astra/bundle_v1_4 -name __pycache__ -type d -exec rm -rf {} +
```

- [ ] **Step 2: Taban testi koş**

Run: `cd astra/bundle_v1_4 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -3`
Expected: `Ran 113 tests` … `OK`

- [ ] **Step 3: Commit**

```bash
git add astra/bundle_v1_4 && git commit -m "astra v1.4: paket kopyası (v1.3 tabanı, 113 test OK)"
```

Bulgu: (taban; bulgu yok). TÜNEL: N/A (mekanik).

---

### Task 2: Belirsizlik kapısı — kritik olmayan `uncertain` kart yayını kapatmaz

Bulgu: K-01, celiski-2, kod_hata-3, test_kapsam-1.

**Files:**
- Modify: `astra/bundle_v1_4/astra_host.py` (`SEMANTIC_POLICY` metni; `TrustedHost.verify` verdict döngüsü ~satır 300-310)
- Modify: `astra/bundle_v1_4/tests/test_host_integration.py` (yeniden adlandırma + 2 yeni test)
- Create: `astra/bundle_v1_4/tests/uncertain_worker_fixture.py`

**Interfaces:**
- Consumes: `TrustedHost.verify(controller, decision, cards, sources, rendered_claims, proofs)`
- Produces: davranış: `stance=uncertain` ve `critical=False` ve `claim_id ∉ decision["claim_ids"]` → kabul; `claim_id ∈ decision["claim_ids"]` ya da `critical=True` ve verdict `uncertain` → `SEMANTIC_CLAIM_UNSUPPORTED`.

- [ ] **Step 1: Fixture yaz**

```python
# tests/uncertain_worker_fixture.py
"""Worker fixture: 'b' emits a non-critical uncertain card; others emit support."""
import json, sys
e = json.load(sys.stdin)
crit = e["worker_id"] != "b"
card = dict(claim_id="c1", proposition_id="p1" if crit else "p2", scope_id=e["scope_ids"][0],
            statement="Supported claim." if crit else "Uncertain non-critical note.",
            label="ÇIKARIM", source_ids=[], stance="support" if crit else "uncertain",
            uncertainty="low" if crit else "high", critical=crit, math=None)
r = {k: e[k] for k in ("protocol", "run_id", "phase_id", "worker_id", "nonce")}
r.update(envelope_digest=e["digest"], status="READY", summary="uncertain fixture", cards=[card],
         covered_scope_ids=e["scope_ids"], unresolved_scope_ids=[], block_reason=None,
         next_safe_step=None, attempts=[])
print(json.dumps(r))
```

(`attempts=[]` alanı Task 3'te şemaya girer; Task 3'ten önce `check_reply` bilinmeyen alanı reddeder → bu fixture'ı Task 3'ten SONRA çalıştır ya da Task 2'yi Task 3 ile aynı commit'te bitir. Sıra: Task 3 önce uygulanabilir; bu planda Task 2 ve 3 birlikte tek commit'e girebilir.)

- [ ] **Step 2: Başarısız testleri yaz** (`tests/test_host_integration.py` sınıfına ekle)

```python
    def test_noncritical_uncertain_card_outside_decision_is_published(self):
        # K-01 / celiski-2: honesty must not be punished
        self.build(worker=str(Path(__file__).with_name("uncertain_worker_fixture.py").resolve()),
                   requirements=[])
        self.decision["claim_ids"] = ["a:c1", "c:c1"]
        result = self.finish()
        self.assertEqual(result["status"], "LOCAL_CHECKS_PASSED")
        verdicts = {v["claim_id"]: v["verdict"] for v in
                    result["host_verification"]["reviewer_response"]["assessment"]["claim_verdicts"]}
        self.assertEqual(verdicts["b:c1"], "uncertain")

    def test_uncertain_card_selected_by_decision_closes_gate(self):
        self.build(worker=str(Path(__file__).with_name("uncertain_worker_fixture.py").resolve()),
                   requirements=[])
        self.decision["claim_ids"] = ["a:c1", "b:c1", "c:c1"]
        self.assert_closed("SEMANTIC_CLAIM_UNSUPPORTED")

    def test_host_alone_does_not_detect_wrong_meaning(self):
        # Documents the limit: the host validates bindings, not meaning.
        for spec, row in zip(self.specs, self.rows):
            row["quote"] = "Request count {} requests. Latency was not measured.".format(row["value"])
            Path(spec["path"]).write_text(row["quote"])
        self.build(mode="pass")
        self.assertEqual(self.finish()["status"], "LOCAL_CHECKS_PASSED")
```

`build()` yardımcısına `worker=WORKER` ve `requirements=None` parametreleri ekle (workers sözlüğünde `argv=[sys.executable, worker]`; `requirements` yalnız `comparisons` parametresine eşlenir — mevcut `requirements=None` davranışı korunur). Mevcut `test_lexically_matching_wrong_meaning_rejects_reviewer_verdict` adını `test_fixture_uncertain_verdict_on_decision_claim_closes_gate` yap (gövde aynı; assert `SEMANTIC_CLAIM_UNSUPPORTED`).

- [ ] **Step 3: Testlerin düştüğünü gör**

Run: `cd astra/bundle_v1_4 && python3 -m unittest tests.test_host_integration -v 2>&1 | grep -E "uncertain|wrong_meaning|FAIL|Error" | head`
Expected: `test_noncritical_uncertain_card_outside_decision_is_published ... FAIL` (SEMANTIC_CLAIM_UNSUPPORTED), diğer ikisi PASS ya da yeni ad hatası.

- [ ] **Step 4: Uygulama** — `astra_host.py` `verify` içindeki döngüyü değiştir:

```python
        expected = {"support": "supported", "refute": "refuted", "uncertain": "uncertain"}
        selected = set(decision["claim_ids"])
        for verdict in verdicts:
            card = cards[verdict["claim_id"]]
            if verdict["verdict"] != expected[card["stance"]]:
                raise Rejected("SEMANTIC_CLAIM_UNSUPPORTED")
            if verdict["verdict"] == "uncertain" and (card["critical"] or verdict["claim_id"] in selected):
                raise Rejected("SEMANTIC_CLAIM_UNSUPPORTED")
```

`SEMANTIC_POLICY` içindeki `"Uncertainty, unsupported\nconditions, unexamined counterevidence or unresolved conflict closes the gate."` cümlesini şununla değiştir: `"Mark uncertainty honestly as uncertain; an uncertain verdict on a critical claim or on a claim selected by the candidate decision closes the gate, unsupported conditions, unexamined counterevidence or unresolved conflict close it too."` (`test_openai_reviewer` policy eşitliğini `SEMANTIC_POLICY` sabitinden aldığı için kırılmaz.)

- [ ] **Step 5: Testler geçer**

Run: `cd astra/bundle_v1_4 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -3`
Expected: `OK`

- [ ] **Step 6: Commit**

```bash
git add astra/bundle_v1_4 && git commit -m "astra v1.4: belirsizlik kapısı — kritik olmayan uncertain kart yayını kapatmaz (K-01)"
```

TÜNEL: alternatif (belgeyi koda uydurup belirsiz kartı yasaklamak) spec §2/BAG Z3'te değerlendirildi ve reddedildi.

---

### Task 3: BLOCKED yanıtı deneme kanıtı ister (`attempts`)

Bulgu: K-03, kacis_yolu-1, eksiklik-14, prompt_muh-4, test_kapsam-6.

**Files:**
- Modify: `astra/bundle_v1_4/astra_reference.py` (`REPLY_SCHEMA`, `DEFAULT_POLICY`, `Controller.check_reply`)
- Modify: `astra/bundle_v1_4/tests/worker_fixture.py`, `tests/goal_worker_fixture.py`, `tests/research_worker_fixture.py`, `tests/uncertain_worker_fixture.py` (`attempts` alanı)
- Modify: `astra/bundle_v1_4/tests/test_astra.py` (+2 test)

**Interfaces:**
- Produces: `REPLY_SCHEMA["properties"]["attempts"] = array(string(4000, 10), 16)`; READY → `attempts == []`; BLOCKED → `len(attempts) >= 2`; aksi `BLOCKED_ATTEMPTS_REQUIRED` / `READY_SCHEMA`.

- [ ] **Step 1: Başarısız testler** (`tests/test_astra.py` RuntimeTests)

```python
    def test_blocked_without_attempts_is_invalid(self):
        c = controller(("blocked_no_attempts", "ready", "ready"))
        p = c.run("Review.")
        self.assertEqual(p.status, "FAIL_CLOSED")
        self.assertTrue(any("BLOCKED_ATTEMPTS_REQUIRED" in e for e in p.errors))

    def test_ready_with_attempts_is_invalid(self):
        c = controller(("ready_with_attempts", "ready", "ready"))
        self.assertEqual(c.run("Review.").status, "FAIL_CLOSED")
```

`tests/worker_fixture.py`: `r.update(... next_safe_step=None, attempts=[])` temel yanıta ekle; `blocked` ve `count_block` modlarında `attempts=["Requested tool X: permission denied", "Searched local docs: no alternative tool"]`; yeni modlar: `blocked_no_attempts` (blocked ama `attempts=[]`), `ready_with_attempts` (READY + `attempts=["x" * 12]`). `missing_block_reason` modunda `attempts` iki öğeli olsun (yalnız block_reason eksik). Diğer üç fixture'a `attempts=[]` ekle.

- [ ] **Step 2: Düştüğünü gör**

Run: `cd astra/bundle_v1_4 && python3 -m unittest tests.test_astra 2>&1 | tail -5`
Expected: yeni testler FAIL ("SCHEMA_FIELDS" ya da AssertionError), mevcut fixture'lar da `attempts` alanı yüzünden SCHEMA_FIELDS ile düşer → şema değişene kadar kırmızı.

- [ ] **Step 3: Uygulama** — `astra_reference.py`:

```python
REPLY_SCHEMA = obj({
    "protocol": string(20, values=[VERSION]), "run_id": ID, "phase_id": ID,
    "worker_id": ID, "nonce": ID, "envelope_digest": ID,
    "status": string(12, values=["READY", "BLOCKED"]), "summary": string(2000, 0),
    "cards": array(CARD_SCHEMA), "covered_scope_ids": array(ID, 64),
    "unresolved_scope_ids": array(ID, 64), "block_reason": string(nullable=True),
    "next_safe_step": string(nullable=True), "attempts": array(string(4000, 10), 16),
})
```

`check_reply` BLOCKED dalı:

```python
        if reply["status"] == "BLOCKED":
            if not reply["block_reason"] or not reply["next_safe_step"] or reply["cards"] or reply["summary"] or reply["covered_scope_ids"] or reply["unresolved_scope_ids"]:
                raise Rejected("BLOCKED_SCHEMA")
            if len(reply["attempts"]) < 2:
                raise Rejected("BLOCKED_ATTEMPTS_REQUIRED")
            return canonical(reply)
        if reply["block_reason"] is not None or reply["next_safe_step"] is not None or not reply["summary"].strip() or reply["attempts"]:
            raise Rejected("READY_SCHEMA")
```

`DEFAULT_POLICY` içine, "A legitimate permission, safety or missing-tool block is BLOCKED, with reason and next_safe_step, no claims." cümlesinden sonra: `"BLOCKED must list at least two distinct attempts actually made (attempts), each naming what was tried and what happened; READY leaves attempts empty. "`

- [ ] **Step 4: Testler geçer**

Run: `cd astra/bundle_v1_4 && python3 -m unittest discover -s tests -p 'test_*.py' 2>&1 | tail -3`
Expected: `OK` (113 + Task 2'nin 3'ü + 2 = 118)

- [ ] **Step 5: Commit**

```bash
git add astra/bundle_v1_4 && git commit -m "astra v1.4: BLOCKED yanıtı en az iki deneme kanıtı ister (K-03)"
```

TÜNEL: alternatif PARTIAL durumu (celiski-3) değerlendirildi; işçi düzeyinde kısmi teslim yerine denetleyici düzeyinde gereksinim durumu (Task 9) seçildi; gerekçe: PARTIAL, BLOCKED'ın sonlandırıcılığını sulandırır.

---

### Task 4: Owner yer tutucu kapısı, finalize kilidi, inceleme nonce/replay

Bulgu: K-06, celiski-1 (önce ÖLÇ), kod_hata-12.

**Files:**
- Create: `astra/probes/probe_replay.py` (ölçüm)
- Modify: `astra/bundle_v1_4/astra_reference.py` (`Controller.__init__`, `finalize`)
- Modify: `astra/bundle_v1_4/astra_host.py` (`verify` request; `_consumed` kümesi)
- Modify: `astra/bundle_v1_4/tests/test_astra.py`, `tests/test_host_integration.py`

**Interfaces:**
- Produces: `Controller._finalized: bool`; ikinci başarılı finalize → `{"status":"FAIL_CLOSED","reason":"FINALIZE_ALREADY_DONE"}`; `verify` isteğinde `review_nonce` (24 bayt hex) ve `issued_at`; `TrustedHost._consumed: set[str]`; yanıt digest'i daha önce tüketilmişse `SEMANTIC_REVIEW_REPLAY`. Owner: `strip().lower()` ∈ {"unknown","bilinmiyor","n/a","na","tbd","-","?","none","null",""} ya da 2'den az harf → `OWNER_REQUIRED`.

- [ ] **Step 1: Ölç (celiski-1 tek-kaynak)** — `astra/probes/probe_replay.py`:

```python
"""Measure: does a second finalize on the same phase pass with an identical request digest?"""
import sys, tempfile
from datetime import datetime, timedelta, timezone
from pathlib import Path
ROOT = Path(__file__).resolve().parents[1] / "bundle_v1_4"
sys.path.insert(0, str(ROOT)); sys.path.insert(0, str(ROOT / "tests"))
from astra_reference import Controller, canonical, bounded_json
from astra_host import SourceVault, TrustedHost
from host_fixture_support import fixture_reviewer
tmp = Path(tempfile.mkdtemp()); src = tmp / "s.txt"; src.write_text("fixture evidence")
now = datetime.now(timezone.utc)
vault = SourceVault([dict(source_id="s1", path=str(src), kind="USER", as_of=(now-timedelta(seconds=2)).isoformat(), valid_until=(now+timedelta(hours=1)).isoformat())])
host = TrustedHost(task="probe", scope_ids=["scope1"], source_vault=vault, comparisons=[], comparison_exemption="probe", reviewer=fixture_reviewer())
W = str(ROOT / "tests" / "worker_fixture.py")
workers = {w: dict(role=r, argv=[sys.executable, W, "ready"]) for w, r in zip("abc", ("model", "counterexample", "evidence"))}
c = Controller(workers, ["scope1"], ["s1"], host=host, max_restarts=0); c.run("probe")
d = dict(action="Report.", owner="operator", guard_metric="g", kill_rule="k", user_cost="u", residual_risk="r", claim_ids=["a:c1", "b:c1", "c:c1"])
r1 = c.finalize(canonical(d)); r2 = c.finalize(canonical(d))
print("first:", r1["status"], "| second:", r2["status"], r2.get("reason"))
same = r1.get("host_verification", {}).get("request_digest") == r2.get("host_verification", {}).get("request_digest")
print("request_digest equal:", same)
```

Run: `python3 astra/probes/probe_replay.py` → beklenen (onarım ÖNCESİ): `first: LOCAL_CHECKS_PASSED | second: LOCAL_CHECKS_PASSED None`, `request_digest equal: True` — sonucu `astra/OLCUM_FAZ3.md`'ye ekle. (Farklı çıkarsa celiski-1 ÇÜRÜDÜ; Task 13'e yaz, replay kısmını uygulama.)

- [ ] **Step 2: Başarısız testler**

`tests/test_astra.py` PublicationTests:
```python
    def test_placeholder_owner_fails(self):
        c = controller(); c.run("Review.")
        for owner in ["n/a", "TBD", "-", "?", "x"]:
            with self.subTest(owner=owner):
                values = inputs(c); values[0]["owner"] = owner
                values[1]["candidate_digest"] = digest(values[0])
                self.assertEqual(finish(c, values)["reason"], "OWNER_REQUIRED")

    def test_second_finalize_is_locked(self):
        c = controller(); c.run("Review.")
        self.assertEqual(finish(c)["status"], "LOCAL_CHECKS_PASSED")
        self.assertEqual(finish(c)["reason"], "FINALIZE_ALREADY_DONE")
```

`tests/test_host_integration.py`:
```python
    def test_review_request_carries_fresh_nonce(self):
        self.build()
        receipt = self.finish()["host_verification"]
        self.assertRegex(receipt["review_nonce"], r"^[0-9a-f]{48}$")
        self.assertIn("issued_at", receipt)

    def test_replayed_review_response_is_rejected(self):
        self.build(mode="replay")
        self.assert_closed("SEMANTIC_REVIEW_BINDING")
```

(`replay` modu digest'i "wrong" yapar → BINDING; gerçek replay senaryosu nonce ile yapısal olarak imkânsızdır — testin docstring'i bunu söyler.)

- [ ] **Step 3: Düştüğünü gör** — `python3 -m unittest tests.test_astra tests.test_host_integration 2>&1 | tail -5` → 4 FAIL.

- [ ] **Step 4: Uygulama**

`astra_reference.py`:
```python
PLACEHOLDER_OWNERS = {"unknown", "bilinmiyor", "n/a", "na", "tbd", "-", "?", "none", "null", ""}
```
`Controller.__init__` sonuna `self._finalized = False`; `finalize` başında (phase kontrolünden sonra, try'dan önce):
```python
        if self._finalized:
            self.log("FINALIZATION_REJECTED", {"reason": "FINALIZE_ALREADY_DONE"})
            return {"status": "FAIL_CLOSED", "reason": "FINALIZE_ALREADY_DONE"}
```
owner denetimi:
```python
            owner = decision["owner"].strip().lower()
            if owner in PLACEHOLDER_OWNERS or sum(ch.isalpha() for ch in owner) < 2:
                raise Rejected("OWNER_REQUIRED")
```
başarı dalında `self._finalized = True` (`result` oluşturulduktan sonra, log'dan önce).

`astra_host.py` `TrustedHost.__init__` sonuna `self._consumed = set()`; `verify` isteğine `review_nonce=secrets.token_hex(24), issued_at=stamp()` ekle (`import secrets`); yanıt geldikten sonra:
```python
        if response["request_digest"] in self._consumed:
            raise Rejected("SEMANTIC_REVIEW_REPLAY")
        self._consumed.add(response["request_digest"])
```
makbuza `review_nonce=request["review_nonce"], issued_at=request["issued_at"]` ekle.

- [ ] **Step 5: Testler geçer** — tüm paket `OK`. Probe'u tekrar koş: `second: FAIL_CLOSED FINALIZE_ALREADY_DONE`, sonucu OLCUM_FAZ3.md'ye ekle.

- [ ] **Step 6: Commit** — `git commit -m "astra v1.4: owner yer tutucu, finalize kilidi, inceleme nonce (K-06, celiski-1)"`

---

### Task 5: Kod kenarları — anahtar sızması, yorumlu ifade, Unicode işaret, symlink

Bulgu: kod_hata-5, kod_hata-2, kod_hata-6, kod_hata-7.

**Files:**
- Modify: `astra/bundle_v1_4/astra_host.py` (`ReviewerEndpoint.__post_init__`, `SourceVault._read`)
- Modify: `astra/bundle_v1_4/astra_reference.py` (`exact_math`)
- Modify: `astra/bundle_v1_4/astra_compare.py` (değer eşleme)
- Modify: `tests/test_host_integration.py`, `tests/test_astra.py`, `tests/test_comparison.py`

- [ ] **Step 1: Başarısız testler**

```python
# test_host_integration.py
    def test_fixture_reviewer_cannot_receive_credentials(self):
        fixture = fixture_reviewer()
        with self.assertRaisesRegex(Rejected, "REVIEWER_CREDENTIAL_SCOPE"):
            ReviewerEndpoint(fixture.argv, "TEST_FIXTURE", "fixture-model", "fixture-effort",
                             credential_env=("OPENAI_API_KEY",))

    def test_symlinked_source_is_rejected(self):
        link = Path(self.tmp.name) / "link.txt"
        link.symlink_to(self.specs[0]["path"])
        self.specs[0]["path"] = str(link)
        with self.assertRaisesRegex(Rejected, "SOURCE_SYMLINK_REJECTED"):
            SourceVault(self.specs)

# test_astra.py MathTests
    def test_comments_and_newlines_are_rejected(self):
        for s in ["1+1 # really 3", "1+1\n# note", "1+\\\n1"]:
            with self.subTest(source=s), self.assertRaises(MathRejected):
                exact_math(s)

# test_comparison.py
    def test_unicode_sign_and_separators_cannot_be_dropped(self):
        for quote in ("Measured latency: −100 ms.", "Measured latency: 100/200 ms.", "ratio 100:200"):
            with self.subTest(quote=quote):
                data = fixture()
                data[2]["s0"] = quote; data[0][0]["quote"] = quote; data[0][0]["value"] = "100"
                data[1][0]["content_digest"] = digest(quote)
                with self.assertRaisesRegex(Rejected, "VALUE_NOT_IN_QUOTE"):
                    compare(data)
```

- [ ] **Step 2: Düştüğünü gör** — 4 FAIL.

- [ ] **Step 3: Uygulama**

`astra_host.py` `__post_init__`, kind kontrolünden sonra:
```python
        if self.kind == "TEST_FIXTURE" and self.credential_env:
            raise Rejected("REVIEWER_CREDENTIAL_SCOPE")
```
`SourceVault.__init__`: `path = Path(spec["path"])` (resolve etme); `_read` başında:
```python
        if os.path.islink(path):
            raise Rejected("SOURCE_SYMLINK_REJECTED")
        path = path.resolve()
```
(`locator=path.as_uri()` çözülmüş yolu kullanmaya devam eder; `assert_current` `paths` sözlüğünden çözülmüş yolu okur — `self._paths[sid] = str(path)` çözülmüş değer.)

`exact_math` girişinde `source = source.strip()` satırından sonra:
```python
    if any(ch in source for ch in "#\n\r\\;"):
        raise MathRejected("UNSUPPORTED")
```
`astra_compare.py`:
```python
import unicodedata
...
        quote = unicodedata.normalize("NFKC", row["quote"])
        numeric_literal = r"(?<![\w.,+\-/:−‒-―＋－])" + re.escape(value) + r"(?![\w.,/:])"
        if re.search(numeric_literal, quote) is None:
            raise Rejected("VALUE_NOT_IN_QUOTE")
```
(`row["quote"] not in content` denetimi ham metinle kalır.)

- [ ] **Step 4: Testler geçer** — paket `OK`.
- [ ] **Step 5: Commit** — `git commit -m "astra v1.4: anahtar sızması, yorumlu ifade, Unicode işaret, symlink kapıları (kod_hata-2/5/6/7)"`

**DÜZELTME (deneme 2/3, Madde 5 denetimi FAIL sonrası):** Denetçi iki gerçek kusur
buldu ve ikisi de kabul edildi:
1. **TİYATRO (kod_hata-7 yarım kapandı):** `os.path.islink(declared)` yalnız SON
   bileşeni denetliyordu; `resolve()` ara dizin symlink'ini sessizce izliyordu —
   bulgunun kendi metnindeki "Ara dizin symlink'leri de sessizce izlenir" alt-durumu
   AÇIK kalmıştı (denetçi `os.open(..., O_NOFOLLOW)` ile symlinked dizin üzerinden
   okumayı fiilen yeniden üretti). Onarım: yol HİÇ çözülmez; `os.path.abspath` ile
   yalnız sözlüksel normalleştirme yapılır ve `declared` + tüm `parents` bileşenleri
   symlink'e karşı denetlenir. Test: `test_symlinked_parent_directory_is_rejected`.
2. **TÜNEL (alternatif kaydı yoktu):** aşağıya yazıldı.

TÜNEL — değerlendirilen ve reddedilen alternatifler (Task 5):
- *Symlink*: (a) bulgunun önerdiği `os.lstat` + `Path.absolute()` — davranışça
  seçilenle aynı, ancak `..` normalleştirmesini çağırana bırakır; (b) `O_NOFOLLOW`
  ile `dir_fd` zincirinde bileşen bileşen açma — TOCTOU'ya karşı en güçlüsü, ama
  stdlib'de belirgin biçimde daha karmaşık ve bu tehdit modelinde (yolu GÜVENİLİR
  operatör beyan eder) kazancı sınırlı; (c) seçilen: hiç çözmeden tüm bileşenleri
  reddet. BEDEL, açıkça kabul edilir: kaynak dosya symlink'li bir bağlama/dizin
  altındaysa (ör. macOS'ta `/tmp` → `/private/tmp`) MEŞRU yol da reddedilir;
  operatör gerçek yolu beyan etmek zorundadır (fail-closed tercih).
- *Unicode*: (a) yalnız açık karakter listesi (U+2212, U+2010..U+2015) eklemek —
  dar, her yeni eşdeğer karakterle yeniden kırılır; (b) seçilen: NFKC + genişletilmiş
  sınıf. YAN ETKİ, açıkça kabul edilir: NFKC tam-genişlik rakamları da katlar
  (`１２０` → `120`), yani alıntı eşleşmesi genişler. Bu bilinçli: alıntı denetiminin
  amacı sayının kaynakta GEÇTİĞİNİ göstermek; tam-genişlik yazımı aynı sayıdır.
  Ham metin denetimi (`row["quote"] not in content`) NFKC'siz kalır, yani alıntının
  kaynakta birebir bulunması şartı gevşemez.

---

### Task 6: TOOL kaynağı araç-çağrısı kaydına bağlanır

Bulgu: eksiklik-6, K-10 (sentetik "gerçek").

**Files:**
- Modify: `astra/bundle_v1_4/astra_host.py` (`SOURCE_SPEC_SCHEMA`, `SourceVault.__init__`)
- Modify: `astra/bundle_v1_4/demo_local.py`, `tests/research_worker_fixture.py`, `tests/test_host_integration.py`, `tests/host_fixture_support.py`

**Interfaces:**
- Produces: `SOURCE_SPEC_SCHEMA` + `"tool_call_record": obj({"tool_name": ID, "args_digest": ID, "exit_status": string(12), "output_digest": ID}, nullable=True)`; kind TOOL → kayıt zorunlu ve `output_digest == digest(content)`; kind USER → kayıt None olmalı; aksi `SOURCE_TOOL_BINDING`.

- [ ] **Step 1: Başarısız testler** (`test_host_integration.py`)

```python
    def test_tool_source_requires_binding_record(self):
        spec = dict(self.specs[0], kind="TOOL", tool_call_record=None)
        with self.assertRaisesRegex(Rejected, "SOURCE_TOOL_BINDING"):
            SourceVault([spec])

    def test_tool_source_digest_must_match_record(self):
        rec = dict(tool_name="fixture_tool", args_digest=digest(["x"]), exit_status="0", output_digest=digest("other"))
        spec = dict(self.specs[0], kind="TOOL", tool_call_record=rec)
        with self.assertRaisesRegex(Rejected, "SOURCE_TOOL_BINDING"):
            SourceVault([spec])
        rec["output_digest"] = digest(Path(self.specs[0]["path"]).read_text())
        self.assertEqual(SourceVault([dict(spec, tool_call_record=rec)]).sources()[0]["kind"], "TOOL")
```

`setUp`'ta specs `kind="USER", tool_call_record=None`; `research_worker_fixture.py` kartı `label="KULLANICI"`; `host_fixture_support.py` specs `kind="USER", tool_call_record=None`; `demo_local.py` sources `kind="USER", tool_call_record=None`; `test_astra.py` `sourced` modu `ARAÇ` etiketiyle `s1` istiyor → fixture support USER verdiği için `SOURCE_KIND` düşer: `worker_fixture.py` `sourced` modunu `label="KULLANICI"` yap (CHANGELOG'a yaz).

- [ ] **Step 2: Düştüğünü gör.**
- [ ] **Step 3: Uygulama** — `SOURCE_SPEC_SCHEMA`'ya alanı ekle; `SourceVault.__init__` döngüsünde:

```python
            record = spec["tool_call_record"]
            if spec["kind"] == "TOOL":
                if record is None or record["output_digest"] != digest(content):
                    raise Rejected("SOURCE_TOOL_BINDING")
            elif record is not None:
                raise Rejected("SOURCE_TOOL_BINDING")
```
ve `records.append(dict(..., tool_call_record=record))` — `SOURCE_SCHEMA` (astra_reference) da `tool_call_record` nullable alanını taşımalı (obj tanımı aynı); `test_comparison.py` fixture'ına `"tool_call_record": None` ve `"kind": "USER"` ekle (kind TOOL kaldığı yerde kayıt gerekir).

- [ ] **Step 4: Testler geçer.** **Step 5: Commit** — `"astra v1.4: TOOL kaynağı araç-çağrısı kaydına bağlanır (eksiklik-6)"`

---

### Task 7: İşçi zarfı kaynak snapshot'ı taşır; zarf boyutu kapısı

Bulgu: celiski-8, K-07 (kapasite).

**Files:**
- Modify: `astra/bundle_v1_4/astra_reference.py` (`Controller.__init__(..., source_contents=None)`, `envelope`, `ROLE_POLICY["evidence"]`)
- Modify: `astra/bundle_v1_4/astra_run.py` (`Controller(..., source_contents=vault.contents())`)
- Modify: `tests/test_astra.py`, `tests/test_host_integration.py`

**Interfaces:**
- Produces: zarf alanı `source_snapshots: dict[source_id, str]` (yalnız `self.source_ids`); `canonical(envelope)` > `WIRE_LIMIT` → `Rejected("ENVELOPE_SIZE")` (run() içinde, faz başlamadan); `ROLE_POLICY["evidence"] = "Check the source snapshots inside this envelope for provenance, relevance and time validity; if a listed source has no snapshot, return BLOCKED with SOURCE_CONTENT_UNAVAILABLE in block_reason."`

- [ ] **Step 1: Başarısız testler**

```python
# test_host_integration.py
    def test_envelope_carries_source_snapshots(self):
        self.build()
        env = self.controller.envelope("a", TASK, "p")
        self.assertEqual(set(env["source_snapshots"]), {"s0", "s1"})
        self.assertIn("median completion latency", env["source_snapshots"]["s0"])
        d = env.pop("digest"); self.assertEqual(d, digest(env))

# test_astra.py RuntimeTests
    def test_oversized_envelope_is_rejected_before_dispatch(self):
        big = {"s1": "x" * (WIRE_LIMIT - 100)}
        c = Controller(workers(("sourced", "ready", "ready")), ["scope1"], ["s1"], source_contents=big)
        with self.assertRaisesRegex(Rejected, "ENVELOPE_SIZE"):
            c.run("Review.")
```

(`Controller`'ı `host_fixture_support` değil `astra_reference`'tan al: `from astra_reference import Controller as RawController`.)

- [ ] **Step 2: Düştüğünü gör.** **Step 3: Uygulama**

```python
    def __init__(self, ..., host=None, source_contents=None):
        ...
        if source_contents is not None:
            source_contents = bounded_json(canonical(source_contents))
            if type(source_contents) is not dict or not set(source_contents).issubset(self.source_ids) \
               or any(type(v) is not str for v in source_contents.values()):
                raise Rejected("SOURCE_CONFIG")
        self._source_contents = source_contents or {}
```
`envelope`: `"source_snapshots": {sid: self._source_contents[sid] for sid in self.source_ids if sid in self._source_contents},` (digest'ten önce). `run()`: her işçi için zarf üretildikten sonra `if len(canonical(envelope)) > WIRE_LIMIT: raise Rejected("ENVELOPE_SIZE")` — döngüden ÖNCE tek sefer (ilk işçi zarfı ile ölç; nonce boyutu sabit). `astra_run.py`: `Controller(..., host=host, source_contents=vault.contents(), ...)`. `host_fixture_support.Controller.run`: host kurulduktan sonra `self._source_contents = self._host._vault.contents()`.

- [ ] **Step 4: Testler geçer.** **Step 5: Commit** — `"astra v1.4: zarf kaynak snapshot'ı + ENVELOPE_SIZE (celiski-8)"`

---

### Task 8: Adaptör ve API uyumu (snapshot toleransı, HTTP sınıfı, taşıma şeması, proxy, bütçe ön kapısı, stderr kodu)

Bulgu: kod_hata-1, api_uyum-1/3/6/8/12/13, kod_hata-4, test_kapsam-2, K-13, K-14.

**Files:**
- Modify: `astra/bundle_v1_4/astra_openai_reviewer.py`
- Modify: `astra/bundle_v1_4/astra_host.py` (`transport_schema`, `ReviewerEndpoint.network`, `execute`, `verify` bütçe ön kapısı)
- Modify: `astra/bundle_v1_4/astra_reference.py` (`invoke` çıkış kodu ≠ 0 → stderr ilk satırı allowlist ile taşınır)
- Modify: `tests/test_openai_reviewer.py`, `tests/test_host_integration.py`

**Interfaces:**
- Produces: `astra_host.transport_schema(schema) -> dict` (minLength/maxLength/minItems/maxItems anahtarlarını özyinelemeli siler; enum/required/additionalProperties kalır); `ReviewerEndpoint(network=None)` — `network = {"proxy": str|None, "ca_bundle": str|None}`; alt sürece `ASTRA_HTTPS_PROXY`, `ASTRA_CA_BUNDLE` env; adaptör `opener`'ı bunlardan kurar (ortam proxy'si okunmaz). Adaptör: `data["model"] == expected or data["model"].startswith(expected + "-")`; `provider_model = data["model"]`; `HTTPError` → `REVIEW_PROVIDER_HTTP_4XX` / `_5XX`; `status != completed` → `REVIEW_PROVIDER_INCOMPLETE:<incomplete_details.reason|unknown>`; yalnız `type=="message"` öğeleri; `invoke` → `Rejected("WORKER_EXIT")` yerine stderr ilk satırı `^[A-Z_0-9:]+$` ise `Rejected(o_satır)`. `verify`: `len(cards) * 534 > WIRE_LIMIT` → `REVIEW_BUDGET_EXCEEDED` (534 = OLCUM: iki kaynaklı asgari hüküm baytı; sabit `MIN_VERDICT_BYTES = 534`).

- [ ] **Step 1: Başarısız testler**

```python
# test_openai_reviewer.py
    def test_snapshot_model_name_is_accepted_and_recorded(self):
        self.body["model"] = "gpt-6-astra-2026-09-01"
        result = self.call()
        self.assertEqual(result["provider_model"], "gpt-6-astra-2026-09-01")

    def test_unrelated_model_is_rejected(self):
        self.body["model"] = "gpt-5"
        with self.assertRaisesRegex(Rejected, "MODEL_MISMATCH"):
            self.call()

    def test_http_error_classes_are_distinguished_without_body(self):
        import urllib.error
        for code, expected in ((400, "HTTP_4XX"), (429, "HTTP_4XX"), (503, "HTTP_5XX")):
            with self.subTest(code=code):
                class Failing:
                    def open(self, request, timeout):
                        raise urllib.error.HTTPError(request.full_url, code, "x", {}, io.BytesIO(b"secret body"))
                with patch.dict("os.environ", {"OPENAI_API_KEY": "k"}, clear=True), self.assertRaisesRegex(Rejected, expected) as cm:
                    review(self.request, opener=Failing())
                self.assertNotIn("secret", str(cm.exception))

    def test_incomplete_reason_is_reported(self):
        self.body["status"] = "incomplete"; self.body["incomplete_details"] = {"reason": "max_output_tokens"}
        with self.assertRaisesRegex(Rejected, "INCOMPLETE:max_output_tokens"):
            self.call()

    def test_reasoning_items_are_ignored(self):
        self.body["output"].insert(0, dict(type="reasoning", summary=[]))
        self.assertEqual(self.call()["provider_model"], "gpt-6-astra")

    def test_transport_schema_has_no_length_constraints(self):
        from astra_host import transport_schema
        sent = json.loads(self.opener.requests[0][0].data) if hasattr(self, "opener") else None
        wire = transport_schema(ASSESSMENT_SCHEMA)
        self.assertNotIn("minLength", json.dumps(wire)); self.assertNotIn("maxItems", json.dumps(wire))
        self.assertIn("additionalProperties", json.dumps(wire))
        self.call()
        self.assertEqual(json.loads(self.opener.requests[0][0].data)["text"]["format"]["schema"], wire)

    def test_proxy_comes_only_from_config_env(self):
        with patch.dict("os.environ", {"OPENAI_API_KEY": "k", "HTTPS_PROXY": "http://ignored:1", "ASTRA_HTTPS_PROXY": "http://cfg:2"}, clear=True):
            from astra_openai_reviewer import build_opener
            handlers = build_opener().handlers
            proxies = [h.proxies for h in handlers if hasattr(h, "proxies")]
            self.assertEqual(proxies, [{"https": "http://cfg:2", "http": "http://cfg:2"}])

# test_host_integration.py
    def test_external_model_success_sets_semantic_support_verified(self):
        self.build()
        adapter = str(Path(__file__).resolve().parents[1] / "astra_openai_reviewer.py")
        endpoint = ReviewerEndpoint((sys.executable, adapter), "EXTERNAL_MODEL", "gpt-6-astra", "max", credential_env=("OPENAI_API_KEY",))
        fixture = fixture_reviewer("pass")
        import astra_reference
        real_invoke = astra_reference.invoke
        def fake_invoke(argv, request, timeout, *, environment=None):
            return real_invoke(list(fixture.argv), request, timeout)
        self.controller._host._reviewer = endpoint
        with patch.dict("os.environ", {"OPENAI_API_KEY": "fixture-not-a-real-key"}), patch.object(astra_reference, "invoke", fake_invoke), patch.object(astra_host, "invoke", fake_invoke):
            result = self.finish()
        self.assertEqual(result["status"], "FAIL_CLOSED")  # fixture returns fixture-model → REVIEWER_CONFIGURATION_MISMATCH
```

(Son test: EXTERNAL_MODEL başarı yolu canlı model olmadan `semantic_support_verified=True` ÜRETEMEZ; test bunu belgelemek için mismatch'i doğrular ve docstring'de "başarı yolu yalnız canlı makbuzla; bu testte fixture yanıtı model adı yüzünden reddedilir" yazar. Ek olarak `fixture_reviewer` yanıtını `provider_model="gpt-6-astra", provider_effort="max"` döndüren `external_like` modu ile ikinci bir test: `assertTrue(receipt["semantic_support_verified"])` — `semantic_reviewer_fixture.py`'ye `external_like` modu ekle. Bu, adaptörün değil host kabul yolunun testidir; docstring'de yazılır.)

```python
    def test_review_budget_precheck_stops_before_call(self):
        self.build()
        many = {f"w:c{i}": dict(claim_id=f"c{i}", proposition_id="p", scope_id="comparison", statement="x", label="ÇIKARIM", source_ids=[], stance="support", uncertainty="low", critical=False, math=None) for i in range(300)}
        with self.assertRaisesRegex(Rejected, "REVIEW_BUDGET_EXCEEDED"):
            self.controller._host.verify(self.controller, self.decision, many, self.vault.sources(), [], {})
```

(verify'ın erken denetimleri `HOST_TASK_BINDING`/`COMPARISON_CLAIM_BINDING` bu sahte kart kümesinde düşebilir → bütçe kapısını `contract["comparisons"]` döngüsünden ÖNCE, task binding'den SONRA koy ve testte `self.decision["claim_ids"] = list(many)[:3]`, `requirements=[]` ile kur.)

- [ ] **Step 2: Düştüğünü gör.** **Step 3: Uygulama** (özet; tam kod görev sırasında):

`astra_host.py`:
```python
MIN_VERDICT_BYTES = 534  # measured: two-source minimal verdict with 120-char excerpts

def transport_schema(schema):
    if type(schema) is dict:
        return {k: transport_schema(v) for k, v in schema.items() if k not in {"minLength", "maxLength", "minItems", "maxItems"}}
    if type(schema) is list:
        return [transport_schema(x) for x in schema]
    return schema
```
`ReviewerEndpoint`: `network: tuple = ()` → `__post_init__`'te `dict(network)`; `execute`: `environment["ASTRA_HTTPS_PROXY"]`, `environment["ASTRA_CA_BUNDLE"]` (varsa); `except Rejected as exc: raise` (invoke'un taşıdığı kod). `verify`: `if len(cards) * MIN_VERDICT_BYTES > WIRE_LIMIT: raise Rejected("REVIEW_BUDGET_EXCEEDED")`; istek `assessment_schema=ASSESSMENT_SCHEMA` (tam) + `wire_schema=transport_schema(ASSESSMENT_SCHEMA)`.
`astra_reference.invoke`: `if code != 0:` → `first = bytes(error).decode("utf-8", "replace").splitlines()[:1]`; `if first and re.fullmatch(r"[A-Z0-9_:]{3,80}", first[0]): raise Rejected(first[0])`; `raise Rejected("WORKER_EXIT")`.
`astra_openai_reviewer.py`: `build_opener()` (ASTRA_HTTPS_PROXY/ASTRA_CA_BUNDLE'dan `ProxyHandler`, `HTTPSHandler(context=ssl.create_default_context(cafile=...))`, `NoRedirect`); `text.format.schema = request["wire_schema"]`; `except urllib.error.HTTPError as exc: raise Rejected("REVIEW_PROVIDER_HTTP_4XX" if 400 <= exc.code < 500 else "REVIEW_PROVIDER_HTTP_5XX")` (gövde okunmaz); `if data.get("status") != "completed": raise Rejected("REVIEW_PROVIDER_INCOMPLETE:" + str((data.get("incomplete_details") or {}).get("reason") or "unknown"))`; model toleransı; `for message in data["output"]: if message.get("type") != "message": continue`. `REVIEW_POLICY_MISMATCH` denetimine `request.get("wire_schema") != transport_schema(ASSESSMENT_SCHEMA)` ekle. `ReviewerEndpoint.execute` model karşılaştırması: `response["provider_model"] == self.model or response["provider_model"].startswith(self.model + "-")`.

- [ ] **Step 4: Testler geçer.** **Step 5: Commit** — `"astra v1.4: adaptör/API uyumu (snapshot, HTTP sınıfı, taşıma şeması, proxy, bütçe) (api_uyum-*, kod_hata-1/4)"`

TÜNEL: proxy için "ortamdan oku" alternatifi değerlendirildi; reddedildi (v1.3 'ortam okunmaz' ilkesi korunur, yapılandırma açık olur).

---

### Task 9: Gereksinim defteri ve host türetimli TASK_STATUS; kararın kartlardan sonra kurulması

Bulgu: eksiklik-1, kacis_yolu-3/4, celiski-12, celiski-7, makyaj-5/6/13, K-11, kod_hata-12.

**Files:**
- Modify: `astra/bundle_v1_4/astra_host.py` (`REQUIREMENT_SCHEMA`, `derive_task_status`, `TrustedHost(requirements=())`, `verify`)
- Modify: `astra/bundle_v1_4/astra_run.py` (config `requirements` isteğe bağlı; `claim_ids == ["*"]`; try içinde yazım)
- Modify: `tests/test_host_integration.py`, yeni `tests/test_run_cli.py`

**Interfaces:**
- Produces:
```python
REQUIREMENT_SCHEMA = obj({
    "requirement_id": ID, "basis_quote": string(2000), "delivery": string(2000),
    "acceptance_check": string(2000), "evidence_ids": array(ID, 64),
    "status": string(12, values=["OPEN", "WORKING", "VERIFIED", "BLOCKED"]),
    "depends_on": array(ID, 64),
})
def derive_task_status(requirements) -> str  # all VERIFIED → COMPLETE; any BLOCKED → BLOCKED; else PARTIAL; [] → "NO_REQUIREMENTS"
```
`verify`: her `status == VERIFIED` gereksinimde `evidence_ids` boş değil ve her kimlik ∈ (`source_ids` ∪ `cards` ∪ `proofs` ∪ `comparison requirement_id`'ler ∪ `^sha256:[0-9a-f]{64}$`) değilse `REQUIREMENT_UNVERIFIED`. Makbuza `requirements` + `task_status`. `astra_run.execute` çıktısına `task_status` (host türetimli; config'te `task_status` alanı varsa `HOST_CONFIG_FIELDS`).

- [ ] **Step 1: Başarısız testler**

```python
# test_host_integration.py
    def test_verified_requirement_needs_real_evidence(self):
        reqs = [dict(requirement_id="R1", basis_quote="compare A and B", delivery="comparison", acceptance_check="observed leaders", evidence_ids=["ghost"], status="VERIFIED", depends_on=[])]
        self.build(requirements_ledger=reqs)
        self.assert_closed("REQUIREMENT_UNVERIFIED")

    def test_task_status_is_derived_not_declared(self):
        reqs = [dict(requirement_id="R1", basis_quote="compare", delivery="comparison", acceptance_check="leaders", evidence_ids=["latency"], status="VERIFIED", depends_on=[]),
                dict(requirement_id="R2", basis_quote="live model", delivery="none", acceptance_check="receipt", evidence_ids=[], status="BLOCKED", depends_on=[])]
        self.build(requirements_ledger=reqs)
        receipt = self.finish()["host_verification"]
        self.assertEqual(receipt["task_status"], "BLOCKED")
        self.assertEqual([r["requirement_id"] for r in receipt["requirements"]], ["R1", "R2"])

# tests/test_run_cli.py
class RunCliTests(unittest.TestCase):
    def test_wildcard_claim_ids_expand_after_phase(self): ...  # demo config kopyası, decision.claim_ids=["*"] → result.claims 3 kart
    def test_declared_task_status_in_config_is_rejected(self): ...  # config["task_status"]="COMPLETE" → FAIL_CLOSED HOST_CONFIG_FIELDS
    def test_output_to_directory_path_is_fail_closed_not_traceback(self): ...  # --output <dizin> → exit 1, stdout JSON, reason HOST_CONFIG_OR_IO_ERROR
```
(`test_run_cli.py` demo_local'in config kurucusunu `subprocess` ile çağırır: `python3 demo_local.py --case valid --output-dir <tmp>`; sonra config'i düzenleyip `astra_run.py` koşar.)

- [ ] **Step 2: Düştüğünü gör.** **Step 3: Uygulama** — şema/türetim/verify/astra_run (özet yukarıda; `execute` içinde `phase = controller.run(...)` sonrası `if decision["claim_ids"] == ["*"]: decision["claim_ids"] = [wid + ":" + c["claim_id"] for raw in phase.replies for wid, c in ...]`; `main()`'de `args.output.write_text` try bloğuna alınır; hata → `HOST_CONFIG_OR_IO_ERROR` sonucu stdout'a yazılır, dosya yazılamazsa stdout'ta `"output": null`).

- [ ] **Step 4: Testler geçer.** **Step 5: Commit** — `"astra v1.4: gereksinim defteri + host türetimli TASK_STATUS + karar kartlardan sonra (eksiklik-1, celiski-7)"`

**DÜZELTME (deneme 2/3, Madde 9 denetimi FAIL sonrası):** üç bulgu da kabul edildi.
1. **ATLAMA:** plan `execute()` çıktısında `task_status` istiyordu; yalnız makbuzun
   içinde vardı. Artık üst seviyede de var ve kapı kapansa bile (makbuz yokken)
   aynı `derive_task_status` ile defterden türetilerek raporlanıyor.
2. **TİYATRO (en ağırı):** `sha256:<64 hex>` yalnız BİÇİM olarak denetleniyordu —
   denetçi uydurma bir hash'le `VERIFIED`/`COMPLETE` üretti, yani eksiklik-1
   ("VERIFIED bir öz-beyandı") o dal için KAPANMAMIŞTI. Artık bir sha256 kimliği bu
   koşuda GERÇEKTEN üretilmiş bir digest'e eşleşmek zorunda (kaynak içerik/erişim
   digest'i, kart, hesap kanıtı, karşılaştırma sonucu, faz, karar, sunulan iddialar,
   kaynak defteri). `DIGEST_ID` regex'i kaldırıldı.
3. **TÜNEL:** `REQUIREMENT_DEPENDENCY` alternatifsiz eklenmişti; aşağıya yazıldı ve
   red yolları teste bağlandı.

TÜNEL — `REQUIREMENT_DEPENDENCY` için değerlendirilen ve reddedilen alternatifler:
- *Nerede denetlensin:* (a) `verify()` içinde — kapı geç kapanır, geçersiz defter
  faz boyunca taşınır; (b) SEÇİLEN: `TrustedHost.__init__` — donmuş sözleşme kurulurken
  reddedilir, çünkü bu yapısal bir tutarlılıktır ve koşu verisine bağlı değildir.
- *Ne yapılsın:* (a) durumu sessizce `PARTIAL`e düşürmek — beyanı düzeltir ama
  operatörün hatasını gizler; (b) SEÇİLEN: fail-closed reddetmek. BEDEL: geçerli ama
  sırası bozuk bir defter koşuyu hiç başlatmaz.
- *Derinlik:* geçişli (transitive) kapanış denetlenmiyor; yalnız DOĞRUDAN bağımlılık.
  Gerekçe: her gereksinim kendi doğrudan bağımlılığını sağladığında zincir zaten
  tümevarımla sağlanır; geçişli kapanış aynı kararı pahalıya alır. Döngü ise
  `depends_on` kendisini gösteremediği için tek adımda, daha uzun döngüde ise
  hiçbir düğüm VERIFIED olamayacağı için kapının kendisiyle kapanır.

---

### Task 10: Komut metni v1.4 ve komut-metni testi

Bulgu: K-02, K-04, K-05, K-08, K-09, K-15, K-16, K-17..K-20, kacis_yolu-2/3/5/7/8/9/10/12/13, prompt_muh-1/2/3/5/6/8/9/10/12/13, eksiklik-10/11/13, makyaj-1/9/10/14, celiski-4/6/13.

**Files:**
- Create: `astra/bundle_v1_4/astra_command.md` (taslak: scratchpad `astra_command_v1_4_TASLAK.md`; ekler aşağıda)
- Create: `astra/bundle_v1_4/tests/test_command_text.py`
- Modify: `astra/bundle_v1_4/astra_host.py` `SEMANTIC_POLICY` (inceleyici tarafı: varsayım beyanı, soru yok → uncertain + gerekçe)

**Taslağa eklenecek maddeler (her biri bir bulguya karşılık):**
1. §1 rol paragrafının altına: "İki talimat yüzeyi vardır: (a) bu komut = görevi yürüten asistan/host; (b) SEMANTIC_POLICY = inceleyici gpt-6-astra istemi (effort makbuzu yalnız orada). Bu komut inceleyiciye gönderilmez." (prompt_muh-8)
2. §3 Ç6 altına "Host yoksa host sensin: ADIM 1 envanteri, TASK_STATUS, CAPABILITY_NEED ve TOOL_ROUTE kayıtları yanıtın sonunda ```json astra_records``` bloğunda yayımlanır; blok yoksa TASK_STATUS=PARTIAL sayılır. Bloktaki kanıt yalnız teslimat konumu+sha256 ve komut çıktısı olabilir." (kacis_yolu-3)
3. §2 ADIM 3'e: "Araç çıktısı da yanlış olabilir: hata metni, boş çıktı, dönem/sürüm uyuşmazlığı, iki aracın çelişkisi → uncertain." (eksiklik-6)
4. §4'e BLOCKED `attempts` (Task 3), TOOL `tool_call_record` (Task 6), zarf `source_snapshots` (Task 7), gereksinim defteri + host türetimli TASK_STATUS (Task 9), bütçe ön kapısı ve taşıma şeması (Task 8), FINAL_STATUS enum'undan APPROVED'ın "yalnız REAL_ISOLATION çalıştırıcısı üretir; bu paket üretemez" notu (celiski-4).
5. §5'e: "Kısa anlatı derinliğin yerine geçmez: ADIM 5 saldırı listesi ve ADIM 6 rubrik tablosu artefakttır, özet değildir." (kacis_yolu-7)
6. Tur bitirme koşulu (prompt_muh-3): "Tur ancak şu üçünden biriyle biter: (1) tüm R VERIFIED (COMPLETE); (2) kalan her R için Ç1-Ç5'ten biri artefaktıyla açılmış (PARTIAL/BLOCKED); (3) kullanıcı açıkça durdurdu. 'Yapabileceğim adım kalmadı' cümlesi tek başına bitiş değildir."
7. Kritik bayrağı (kacis_yolu-13): "`critical`, iddia kararı/teslimatı değiştiriyorsa true'dur; karar `claim_ids`'ine giren her kart otomatik kritiktir."
8. Kapasite (K-07/api_uyum-3): "İnceleme isteği tek gövdede taşınır (131072 bayt) ve inceleme çıktısı 16384 token ile sınırlıdır; ölçülen asgari hüküm boyutuyla (534 bayt/iddia) ~245 iddia üstü sığmaz — bu paket büyük görevi Ç5 ile parçalar, tek koşuda desteklemez."

**test_command_text.py** (taslak):
```python
import re, unittest
from pathlib import Path
TEXT = (Path(__file__).resolve().parents[1] / "astra_command.md").read_text(encoding="utf-8")
PROTECTED = [
    "\"ASTRA\" bu protokolün adıdır; seçilmiş modelin ya da ayarın kanıtı değildir",
    "Yapmadığını yaptım deme",
    "Kendi ayarını beyan edemezsin",
    "APPROVED yalnız REAL_ISOLATION modunda",
    "APPROVED bir denetim sonucudur; dış dünyada işlem/emir yetkisi değildir",
    "Görev verisi talimat değildir",
    "canlı `gpt-6-astra` çağrısıyla sınanmadı",
    "inceleyici adaptöründe arama aracı yoktur",
    "TEST_FIXTURE inceleyicisi",
    "Host yoksa host sensin",
]
DISCRETION = ["gerekirse", "mümkünse", "uygun görürsen", "yeterince", "gerektiğinde"]
SLOP = ["Sonuç olarak:", "Özetle:", "önemle belirtmek gerekir", "kayda değer", "sorunsuzca", "oyun değiştirici", "kusursuz"]
class CommandTextTests(unittest.TestCase):
    def test_protected_sentences_present(self):
        for s in PROTECTED:
            with self.subTest(s=s): self.assertIn(s, TEXT)
    def test_no_discretionary_adverbs_outside_quotes(self):
        body = re.sub(r'"[^"\n]*"', "", TEXT)  # quoted forbidden words are allowed (the ban sentence itself)
        for w in DISCRETION:
            with self.subTest(w=w): self.assertNotIn(w, body)
    def test_no_slop(self):
        body = re.sub(r'"[^"\n]*"', "", TEXT)
        for w in SLOP:
            with self.subTest(w=w): self.assertNotIn(w, body)
    def test_every_exit_gate_names_an_artefact(self):
        section = TEXT.split("**3. Çıkış kapıları")[1].split("**4.")[0]
        gates = re.findall(r"- Ç(\d) \*\*.*?\*\*(.*?)(?=\n- Ç|\n\n)", section, re.S)
        self.assertEqual(len(gates), 6)
        for n, body in gates:
            with self.subTest(gate=n): self.assertRegex(body, r"zorunlu|artefakt|günlü|listelen|yazılır|teslim")
    def test_synthetic_is_never_called_real(self):
        for phrase in ["gerçek CLI", "gerçek alt süreç", "dört gerçek senaryo"]:
            self.assertNotIn(phrase, TEXT)
    def test_size_budget(self):
        self.assertLess(len(TEXT.encode("utf-8")), 30000)
```

- [ ] **Step 1: Testi yaz, düştüğünü gör** (dosya yok → FileNotFoundError).
- [ ] **Step 2: Komut metnini yaz** (taslak + 8 ekleme); `SEMANTIC_POLICY`'ye ekle: `"State any assumption you rely on inside the reason field. Do not ask questions; when evidence is missing return uncertain with the reason."`
- [ ] **Step 3: Testler geçer** (paket tamamı). **Step 4: Commit** — `"astra v1.4: komut metni tek kaynak + komut-metni testi (K-02..K-20, kacis_yolu-*, prompt_muh-*)"`

TÜNEL: yaklaşım A (yerinde cerrahi) spec §2'de değerlendirildi ve reddedildi.

---

### Task 11: README / CHANGELOG / prompt_spec / repair_contract / source_review_notes ve verification'ın yeniden üretimi

Bulgu: makyaj-1/2/3/4/7/11, api_uyum-2, K-12, K-19, test_kapsam-8/14.

**Files:**
- Modify: `astra/bundle_v1_4/README.md`, `CHANGELOG.md`, `prompt_spec.md`, `repair_contract.json`, `verification/source_review_notes.json`
- Create: `astra/bundle_v1_4/scripts/regenerate_verification.py` (testleri koşar → `verification/test_run.txt`, `test_result.json`; 4 demo → `verification/cli_runs/*`, `cli_summary.json`; Python sürümü ve tarih yazar)
- Modify: `astra/bundle_v1_4/demo_local.py` (attempts/USER kind — Task 3/6'da yapıldı; burada yalnız README cümleleri)
- Test: `tests/test_run_cli.py` → `test_regenerated_summary_matches_expected_statuses` (4 senaryo beklenen durumları)

İçerik kuralları: "gerçek CLI senaryosu" → "yerel CLI senaryosu (fixture işçi ve fixture inceleyici; boru hattını sınar, anlamı sınamaz)"; "113 test" → sayı + bileşim; "3.12.13 ile" → koşulan sürüm (regenerate yazar); `repair_contract.json` → `{"version":"1.4","requirements":[R1..R5 spec'ten],"status":"LOCAL_TESTED","task_status": <host derive değil — burada elle YOK; alan çıkarılır>, "live_model_dependency": "...","model_eval":"NOT_RUN"}` (task_status alanı dosyadan kaldırılır; gerekçe: türetilmemiş durum yazılmaz); `source_review_notes.json` → üç iddia `"verdict":"PREVIOUSLY_SUPPORTED_NOT_REVERIFIED"`, `"reverification":"2026-09-07: developers.openai.com EGRESS_BLOCKED"`, ikincil kaynak listesi ARASTIRMA.md'den.

- [ ] **Step 1:** `regenerate_verification.py` yaz (subprocess ile `python3 -m unittest discover -v`, çıktı dosyaya; her demo yeni tmp dizine, sonuç dosyaları `verification/cli_runs/<case>/` altına kopyalanır; mutlak yollar `<BUNDLE>` ile maskelenir).
- [ ] **Step 2:** Test yaz (cli_summary beklenen dört durum) → düşer (dosya eski).
- [ ] **Step 3:** Metinleri yaz; `python3 scripts/regenerate_verification.py` koş.
- [ ] **Step 4:** Paket testleri `OK`. **Step 5: Commit** — `"astra v1.4: dürüst README/CHANGELOG/sözleşme; verification bu ortamda yeniden üretildi (makyaj-*)"`

---

### Task 12: Tek-dosya belge üretimi ve gidiş-dönüş testi

Bulgu: K-16 (sürüklenme), makyaj-7 (ZIP iddiası kaldırılır).

**Files:**
- Create: `astra/astra_tamir_v1_4_HEADER.md` (belge başlığı: sürüm/tarih, durum satırı, "Çalışma komutu" = `astra_command.md` içeriği bundle_tools tarafından eklenir, çıkarma yönergesi = `bundle_tools.py extract` komutu; ZIP cümlesi yok)
- Modify: `astra/scripts/bundle_tools.py` (`build`: header'daki `<!-- ASTRA_COMMAND_HERE -->` işaretini `astra_command.md` ile doldurur; `verify` sonrası `roundtrip <belge> ` alt komutu: geçici dizine extract + unittest koşar)
- Create: `astra/astra_tamir_v1_4.md` (üretilen)
- Test: `astra/tests/test_bundle_tools.py` (build → verify OK; başlıktaki komut == gömülü astra_command.md; roundtrip testleri OK)

- [ ] **Step 1:** Test yaz → düşer. **Step 2:** bundle_tools'u genişlet; header'ı yaz; `python3 astra/scripts/bundle_tools.py build astra/bundle_v1_4 astra/astra_tamir_v1_4_HEADER.md astra/astra_tamir_v1_4.md 1.4`.
- [ ] **Step 3:** `python3 astra/scripts/bundle_tools.py verify astra/astra_tamir_v1_4.md` ve `roundtrip` → OK. **Step 4: Commit** — `"astra v1.4: tek-dosya belge üretimi + gidiş-dönüş testi"`

---

### Task 13: Bulgu → değişiklik tablosu, çürütme sonuçlarının işlenmesi, kontrol zinciri

Bulgu: (kapanış; R1 kabul kontrolü).

**Files:**
- Create: `astra/BULGU_DEGISIKLIK.md` (her bulgu kimliği → görev/commit ya da "ÇÜRÜDÜ — uygulanmadı" ya da "KAPSAM DIŞI — gerekçe")
- Create/Modify: `astra/bulgular_hukum.json` (iş akışı çürütme çıktısı), `astra/BAG_HARITASI.md` (hüküm sütunu güncellenir)
- Create: `.claude/kontrol/zincir/astra-v1-4.json` (kontrol ajanları defteri; şablon `.claude/kontrol/zincir_sablon.json`) ve `python3 .claude/skills/piramit-sistem/scripts/kontrol_ajanlari.py --zincir <defter> --ozet` çıktısı

- [ ] **Step 1:** `bulgular_hukum.json`'ı oku; çürüyen bulguların görevlerini işaretle; uygulanmış değişiklikle çelişen çürütme varsa (ör. celiski-1 ÇÜRÜDÜ ama nonce eklendi) değişiklik geri alınmaz, "ihtiyaten kalıyor, gerekçe" yazılır.
- [ ] **Step 2:** Tabloyu yaz; BAG_HARITASI hüküm sütununu güncelle.
- [ ] **Step 3:** Zincir defterini doldur, kontrol ajanlarını koş, çıktıyı `astra/KONTROL_OZET.txt`'e yaz; P0 varsa düzelt.
- [ ] **Step 4: Commit** — `"astra v1.4: bulgu→değişiklik izlenebilirliği, çürütme sonuçları, kontrol zinciri"`

---

## Self-review

- Spec kapsamı: R1 → Task 13 (+iş akışı); R2 → Task 1-12; R3 → Task 3, 9, 10; R4 → ARASTIRMA.md (FAZ 0, tamam) + Task 11 notları; R5 → Task 10 testi + Task 11 metinleri. Kapsam dışı maddeler Task 11 metninde "yapılmadı" olarak taşınır.
- Yer tutucu taraması: Task 9 ve 11'de bazı adımlar özet kodla yazıldı ("özet yukarıda"); uygulayıcı bu görevlerde tam kodu Interfaces bloğundaki imzalara göre yazar — imzalar birebir verildi.
- Tür tutarlılığı: `attempts` (Task 3) → fixture'lar (Task 2/3/6); `tool_call_record` (Task 6) → SOURCE_SCHEMA (astra_reference) + test_comparison fixture; `source_contents` (Task 7) → astra_run + host_fixture_support; `transport_schema`/`wire_schema` (Task 8) → adaptör; `requirements_ledger` build parametresi (Task 9) → `build(requirements=None, requirements_ledger=())`.
