[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "konsey-engine"
version = "1.0.0"
description = "KONSEY bagimsiz kanit kaydi ve yayin kapisi + canli yon/giris-cikis sinyali"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
konsey = "konsey_engine.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
